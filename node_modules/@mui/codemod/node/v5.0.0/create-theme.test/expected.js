"use strict";

var _core = require("@material-ui/core");

var _styles = require("@material-ui/core/styles");

const lightTheme = (0, _styles.createTheme)();
const darkTheme = (0, _styles.createTheme)({
  palette: {
    mode: 'dark'
  }
});